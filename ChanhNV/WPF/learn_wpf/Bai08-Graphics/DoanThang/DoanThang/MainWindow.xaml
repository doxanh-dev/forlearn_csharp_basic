<Window x:Class="DoanThang.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DoanThang"
        mc:Ignorable="d"
        Title="Đoạn thẳng - Line" Height="338" Width="324">
    <Grid>
        <Canvas Height="300" Width="300">
            <!-- Vẽ một đoạn thẳng nằm xiên từ tọa độ (10,10) tới (50,50).Độ dày đoạn thẳng là 4 pixel mà có màu đen-->
            <Line X1="10" Y1="10" X2="50" Y2="50" Stroke="Black" StrokeThickness="4" />

            <!-- Vẽ một đoạn thẳng nằm ngang từ tọa độ (10,50) to (150,50). nằm cách lề trái của canvas 100 pixel 
            Đoạn thẳng màu xanh da trời, độ dày 4 pixel, nét đứt xen kẽ cứ mỗi đoạn màu xanh là 4 thì lại xen khoảng trắng là 1.-->
            <Line X1="10" Y1="50" X2="150" Y2="50" Canvas.Left="100" Stroke="Blue" StrokeThickness="4" StrokeDashArray="4 1" />
        
        
        </Canvas>
            
    </Grid>
</Window>
