<Window x:Class="DataTemplateDonGian.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataTemplateDonGian"
        mc:Ignorable="d"
        Title="MainWindow" Height="360" Width="530"
        WindowStartupLocation="CenterScreen"
        >
    <Window.Resources>
        <!--Định nghĩa nguồn dữ liệu-->
        <local:Staffs x:Key="MyStaffList"/>
    </Window.Resources>
    <Grid>

        <StackPanel>
            <StackPanel.Resources>
                <!--Định nghĩa cách hiển thị mục dữ liệu thông qua một file xaml riêng rẽ-->
                <ResourceDictionary>
                    <ResourceDictionary.MergedDictionaries>
                        <ResourceDictionary Source="/StaffDataTemplate.xaml" />
                    </ResourceDictionary.MergedDictionaries>
                </ResourceDictionary>
            </StackPanel.Resources>
            <!-- The Person-items -->
            <ItemsControl x:Name="personItems" HorizontalAlignment="Stretch" Margin="10" VerticalAlignment="Center" Background="Orange" ItemsSource="{Binding Source={StaticResource MyStaffList}, Path=StaffList}" />
        </StackPanel>

    </Grid>
</Window>
